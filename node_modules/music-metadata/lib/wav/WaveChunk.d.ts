/// <reference types="node" />
import * as Token from "token-types";
import { IChunkHeader } from "../riff/RiffChunk";
/**
 * "fmt"  sub-chunk describes the sound data's format
 * Ref: http://soundfile.sapp.org/doc/WaveFormat
 */
export interface IFormat {
    /**
     * PCM = 1 (i.e. Linear quantization). Values other than 1 indicate some form of compression.
     */
    audioFormat: number;
    /**
     * Mono = 1, Stereo = 2, etc.
     */
    numChannels: number;
    /**
     * 8000, 44100, etc.
     */
    sampleRate: number;
    byteRate: number;
    blockAlign: number;
    bitsPerSample: number;
}
/**
 * format chunk; chunk-id is "fmt "
 * http://soundfile.sapp.org/doc/WaveFormat/
 */
export declare class Format implements Token.IGetToken<IFormat> {
    len: number;
    constructor(header: IChunkHeader);
    get(buf: Buffer, off: number): IFormat;
}
